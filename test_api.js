const crypto = require('crypto'); const secret = '55d0057c326d49b23894b55990ad2928c4daa2fe6a17fe4350a6a8c32195f8ef'; const header = Buffer.from(JSON.stringify({alg:'HS256',typ:'JWT'})).toString('base64url'); const payload = Buffer.from(JSON.stringify({sub:1,iat:Math.floor(Date.now()/1000),exp:Math.floor(Date.now()/1000)+3600})).toString('base64url'); const signature = crypto.createHmac('sha256', secret).update(header + '.' + payload).digest('base64url'); const token = header + '.' + payload + '.' + signature; fetch('http://localhost:8080/api/v1/admin/menus/me', { headers: { Authorization: 'Bearer ' + token } }).then(res => res.json()).then(console.log).catch(console.error);
