const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const unit = await prisma.organizationUnit.findUnique({ where: { id: 31 }, include: { unitDomains: { include: { domain: { include: { translations: { where: { langCode: 'vi' } } } } } } } }); console.log('PRISMA_RESULT:', JSON.stringify(unit, null, 2)); } main().catch(console.error).finally(() => prisma.());
