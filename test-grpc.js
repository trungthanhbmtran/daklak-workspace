const grpc = require('@grpc/grpc-js'); const protoLoader = require('@grpc/proto-loader'); const path = require('path'); const PROTO_PATH = path.join('/app', 'shared/protos/users/organization.proto'); const packageDefinition = protoLoader.loadSync(PROTO_PATH, { keepCase: true, longs: String, enums: String, defaults: true, oneofs: true }); const org_proto = grpc.loadPackageDefinition(packageDefinition).users.organization; const client = new org_proto.OrganizationService('user-service:50051', grpc.credentials.createInsecure()); client.GetUnitScope({ id: 31 }, (err, response) => { if (err) console.error('ERROR:', err); else console.log('SUCCESS:', response); });
