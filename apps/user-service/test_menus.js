const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient({datasources: { db: { url: 'mysql://root:Root123456@localhost:3306/admin_systems' } }}); async function main() { const userId = 1; const user = await prisma.user.findUnique({ where: { id: userId }, include: { roles: { include: { policies: { include: { resource: true } } } } } }); const allowedResources = new Set(); for (const role of user?.roles ?? []) { for (const p of role.policies ?? []) { if (p.resource?.code) allowedResources.add(p.resource.code); } } console.log('Allowed Resources:', Array.from(allowedResources)); } main().catch(console.error).finally(() => prisma.$disconnect());
