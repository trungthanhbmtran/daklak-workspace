generator client {
  provider = "prisma-client-js"
  output       = "../../src/generated/prisma"
  moduleFormat = "cjs"
  previewFeatures = ["prismaSchemaFolder"]
}

datasource db {
  provider = "mysql"
}
