generator client {
  provider = "prisma-client-js"
  engineType = "binary"
  output       = "../../src/generated/prisma"
  moduleFormat = "cjs"
  previewFeatures = ["prismaSchemaFolder"]
}

datasource db {
  provider = "mysql"
}
