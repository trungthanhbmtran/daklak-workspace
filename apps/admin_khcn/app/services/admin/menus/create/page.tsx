import { MenuForm } from "@/features/system-admin/menus";

export const metadata = {
  title: "Thêm mới Menu | Quản trị Hệ thống",
};

export default function CreateMenuPage() {
  return <MenuForm />;
}
