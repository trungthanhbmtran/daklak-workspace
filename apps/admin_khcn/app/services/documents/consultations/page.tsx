import { ConsultationsClient } from "@/features/document";

export const metadata = {
  title: "Lấy ý kiến dự thảo | Quản lý Văn bản",
};

export default function ConsultationsPage() {
  return <ConsultationsClient />;
}