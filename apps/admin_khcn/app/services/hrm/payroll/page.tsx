import { PayrollListClient } from "@/features/hrm";

export const metadata = {
  title: "Quản lý lương | Quản lý Nhân sự",
};

export default function PayrollPage() {
  return <PayrollListClient />;
}
