import { PlanFormClient } from "@/features/hrm/components/plans/PlanFormClient";

export const metadata = {
  title: "Tạo Kế hoạch mới | HRM",
  description: "Tạo kế hoạch tổng",
};

export default function CreatePlanPage() {
  return <PlanFormClient />;
}
