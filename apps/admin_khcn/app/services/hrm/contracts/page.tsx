import { ContractListClient } from "@/features/hrm";

export const metadata = {
  title: "Hợp đồng lao động | Quản lý Nhân sự",
};

export default function ContractsPage() {
  return <ContractListClient />;
}
