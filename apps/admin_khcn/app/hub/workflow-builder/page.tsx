import { WorkflowBuilderClient } from "@/features/hub";

export const metadata = {
  title: "Trình kéo thả Quy trình | Cổng Ứng dụng Nội bộ",
};

export default function WorkflowPage() {
  return <WorkflowBuilderClient />;
}
